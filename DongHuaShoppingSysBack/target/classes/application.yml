server:
  port: 9510

spring:
  datasource:
    username: root
    password: 123456
    url: jdbc:mysql:///shopping_sys

  redis:
    port: 6379
    host: localhost

logging:
  level:
    com.dhu: debug

mybatis-plus:
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
